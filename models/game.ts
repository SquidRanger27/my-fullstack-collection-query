export interface Game {
    id: number
    game: string
    developer: string
    year: number 
  }
  
  export interface NewGame {
    id: number
    game: string
    developer: string
    year: number 
  }
